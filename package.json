{
  "name": "noctem-technology-site",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "astro check",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "assets:generate": "node scripts/generate-assets.mjs",
    "assets:capture": "node scripts/capture-portfolio-assets.mjs",
    "assets:cinema": "node scripts/generate-cinema-assets.mjs",
    "qa:capture": "node scripts/capture-visual-qa.mjs",
    "validate": "npm run check && npm run test && npm run build",
    "validate:full": "npm run validate && npm run test:e2e",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/geist": "^5.3.0",
    "@fontsource-variable/geist-mono": "^5.3.0",
    "@fontsource/instrument-serif": "^5.3.0",
    "astro": "^7.2.8",
    "gsap": "^3.15.0",
    "lenis": "^1.3.26"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "^1.62.1",
    "d3-geo": "^3.1.1",
    "sharp": "^0.35.4",
    "topojson-client": "^3.1.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11",
    "world-atlas": "^2.0.2"
  }
}
